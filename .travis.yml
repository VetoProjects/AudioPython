language: python

python:
  - "2.6"
  - "2.7"

install:
    - sudo pip install cython

script: 
    - python setup.py install
    - nosetests
    - make clean && make cython && make all && sudo make install && sudo make uninstall && make clean && make clean
